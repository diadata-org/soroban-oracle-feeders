{
  "name": "@repo/common",
  "version": "0.0.0",
  "license": "UNLICENSED",
  "private": true,
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "type": "module",
  "files": [
    "build/**"
  ],
  "scripts": {
    "build": "tsc && mkdir -p build/midnight/managed/oracle && cp -r src/midnight/managed/oracle/* build/midnight/managed/oracle/",
    "clean": "rimraf build",
    "dev": "tsc -w",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "peerDependencies": {
    "@stellar/stellar-sdk": "^13.0.0",
    "rxjs": "^7.8.1"
  },
  "dependencies": {
    "@alephium/web3": "^1.0.6",
    "@alephium/web3-wallet": "^1.0.6"
  },
  "devDependencies": {
    "@kadena/client": "^1.8.1",
    "@kadena/types": "^0.6.0",
    "@repo/typescript-config": "*",
    "@types/node": "^20.10.5",
    "@vitest/coverage-v8": "^3.0.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3",
    "vitest": "^3.0.0"
  },
  "packageManager": "yarn@1.22.19",
  "engines": {
    "node": "^22.x",
    "npm": "^10.x"
  }
}
